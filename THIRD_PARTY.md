# Composants tiers

Les scripts `mvnw`, `mvnw.cmd` et `.mvn/wrapper/maven-wrapper.properties` sont générés par Apache Maven Wrapper Plugin 3.3.2, projet de l'Apache Software Foundation, sous licence Apache 2.0. Le texte de cette licence est conservé dans `licenses/Apache-2.0.txt` et leurs en-têtes sont préservés.

Les dépendances de build/runtime sont déclarées et versionnées dans `pom.xml` ; leurs mentions de licence sont incluses dans le JAR assemblé. Les artefacts `com.example.*` du laboratoire sont uniquement des fixtures fictives.

Aucune licence de diffusion du code original de ce dépôt privé n'est attribuée par ce fichier.
